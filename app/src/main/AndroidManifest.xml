<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Wear OS watch face -->
    <uses-feature android:name="android.hardware.type.watch" />

    <!-- WFF is resource-only, no code execution -->
    <application
        android:allowBackup="true"
        android:hasCode="false"
        android:icon="@drawable/preview"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- Watch Face Service declaration for WFF -->
        <service
            android:name="androidx.wear.watchface.WatchFaceService"
            android:directBootAware="true"
            android:exported="true"
            android:permission="android.permission.BIND_WALLPAPER">

            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>

            <meta-data
                android:name="com.google.android.wearable.watchface.wffVersion"
                android:value="2" />

            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face_info" />
        </service>

        <!-- Watch face metadata provider -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

    </application>

</manifest>
